// JS implementation of an Enum for setting the active screen in the Math Facts Quiz app
export const SCREENS = Object.freeze({
  QUIZ_SETTINGS: 'QuizSettings',
  QUIZ: 'Quiz',
  QUIZ_RESULTS: 'QuizResults',
});